{% extends 'base.html.twig' %}

{% block body %}
<h1>Product</h1>

    <table>
        <tbody>
                <tr>
                    <th>Name</th>
                    <th>{{ product.name }}</th>
                </tr>
                 <tr>
                    <th>Description</th>
                    <th>{{ product.description}}</th>
                </tr>
                 <tr>
                      <th>Size</th>
                    <th>{{ product.size }}</th>
                </tr>
        </tbody>
    </table>
{% endblock %}
